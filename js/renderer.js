// Rendering Engine (`renderer.js`)
// This module is responsible for all drawing on the HTML canvas.
// It takes the state from the Simulation Engine and visually represents it.
console.log("renderer.js loaded");
